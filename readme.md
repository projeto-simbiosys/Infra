# Infraestrutura do projeto SIMBIOSYS

Para rodar essa infraestrutura siga o seguinte roteiro abaixo:
<br>
<br>

#### Crie uma chave .pem na raiz do projeto com o comando:

```sh
ssh-keygen -m PEM -t rsa -b 4096 -f key_simbiosys.pem
```
